import styled from 'styled-components';

export const Button = styled.button`
  height: auto;
  border: none;
  outline: none;
  background: transparent;
  border-radius: 8px;

  &:hover {
    opacity: 0.8;
  }
`;
